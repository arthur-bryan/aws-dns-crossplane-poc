id: commit
name: Commit changes
action: github:extras:commit
input:
  folder: entities
  message: "add or edit entity ${{ parameters.name }}"
  gitAuthorName: ${{ user.entity.spec.profile.displayName | default(user.entity.metadata.name) }}
  gitAuthorEmail: ${{ user.entity.spec.profile.email }}
