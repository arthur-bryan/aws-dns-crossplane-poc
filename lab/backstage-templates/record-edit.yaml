apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: aws-dns-record-edit
  title: Edit AWS Route53 DNS Record
  description: Edit an existing AWS Route53 DNS record
  labels:
    category: resources
    ape: "true"
    sre: "true"
    techlead: "true"
    developer: "true"
  tags:
    - resource
    - aws
    - dns
    - route53
    - hidden
spec:
  owner: group:application-platform-engineering
  type: resource

  parameters:
    - title: Target Record
      description: Identifies which record XR to edit. Immutable fields are disabled once the edit form opens.
      required:
        - system
        - environment
        - zoneName
        - recordName
        - type
      properties:
        system:
          title: System
          type: string
          description: System that owns the record
          ui:field: EntityPicker
          ui:options:
            catalogFilter:
              - kind: System
        environment:
          title: Environment
          type: string
          enum: [dev, hml, prd]
          default: dev
        zoneName:
          title: Parent Zone
          type: string
          pattern: '^([a-z0-9]+(-[a-z0-9]+)*\.)+[a-z]{2,}$'
          description: Parent zone FQDN (no trailing dot, immutable)
          ui:help: "Example: example.com"
        recordName:
          title: Record Name
          type: string
          minLength: 1
          maxLength: 253
          description: Short record name (immutable)
          ui:help: "Example: api-test → api-test.example.com"
        type:
          title: Record Type
          type: string
          description: Record type (immutable)
          enum: [A, AAAA, CNAME, TXT, ALIAS]

      dependencies:
        type:
          oneOf:
            - properties:
                type: { const: A }
                ttl:
                  title: TTL (seconds)
                  type: integer
                  default: 3600
                  minimum: 60
                  maximum: 86400
                values:
                  title: IPv4 Addresses
                  type: array
                  items: { type: string, pattern: '^([0-9]{1,3}\.){3}[0-9]{1,3}$' }
                  minItems: 1
              required: [values]
            - properties:
                type: { const: AAAA }
                ttl: { title: TTL (seconds), type: integer, default: 3600, minimum: 60, maximum: 86400 }
                values:
                  title: IPv6 Addresses
                  type: array
                  items: { type: string }
                  minItems: 1
              required: [values]
            - properties:
                type: { const: CNAME }
                ttl: { title: TTL (seconds), type: integer, default: 3600, minimum: 60, maximum: 86400 }
                values:
                  title: Target Hostname
                  type: array
                  items: { type: string }
                  minItems: 1
                  maxItems: 1
              required: [values]
            - properties:
                type: { const: TXT }
                ttl: { title: TTL (seconds), type: integer, default: 3600, minimum: 60, maximum: 86400 }
                values:
                  title: Text values
                  type: array
                  items: { type: string }
                  minItems: 1
              required: [values]
            - properties:
                type: { const: ALIAS }
                serviceType:
                  title: AWS Service Type
                  type: string
                  description: Immutable — matches the existing record's service type
                  enum: [CloudFront, ALB, NLB, S3Website, APIGateway, GlobalAccelerator, Custom]
                dnsName:
                  title: Target DNS Name
                  type: string
                evaluateTargetHealth:
                  title: Evaluate Target Health
                  type: boolean
                  default: false
              required: [serviceType, dnsName]

    - title: Weighted Routing (Optional)
      properties:
        enableWeightedRouting:
          title: Enable Weighted Routing
          type: boolean
          default: false
      dependencies:
        enableWeightedRouting:
          oneOf:
            - properties:
                enableWeightedRouting: { const: false }
            - properties:
                enableWeightedRouting: { const: true }
                setIdentifier:
                  title: Set Identifier
                  type: string
                  minLength: 1
                  maxLength: 128
                weight:
                  title: Weight (0-255)
                  type: integer
                  minimum: 0
                  maximum: 255
              required: [setIdentifier, weight]

  steps:
    - id: fetchSystem
      name: Fetch System entity
      action: catalog:fetch
      input:
        entityRef: ${{ parameters.system }}

    - id: fetchExisting
      name: Download existing Record YAML
      action: fetch:plain
      input:
        url: >-
          https://github.com/arthur-bryan/aws-dns-crossplane-poc/raw/feature/ape-platform-alignment/entities/environments/${{
            steps.fetchSystem.output.entity.metadata.annotations['dock.tech/domain']
          }}/${{
            steps.fetchSystem.output.entity.metadata.annotations['dock.tech/subdomain']
          }}/${{
            steps.fetchSystem.output.entity.metadata.annotations['dock.tech/system']
          }}/${{ parameters.environment }}/resources/aws/record-${{ parameters.recordName }}.${{ parameters.zoneName }}.yaml
        targetPath: >-
          entities/environments/${{
            steps.fetchSystem.output.entity.metadata.annotations['dock.tech/domain']
          }}/${{
            steps.fetchSystem.output.entity.metadata.annotations['dock.tech/subdomain']
          }}/${{
            steps.fetchSystem.output.entity.metadata.annotations['dock.tech/system']
          }}/${{ parameters.environment }}/resources/aws

    - id: mergeChanges
      name: Merge edit into existing YAML
      action: roadiehq:utils:merge
      input:
        path: >-
          entities/environments/${{
            steps.fetchSystem.output.entity.metadata.annotations['dock.tech/domain']
          }}/${{
            steps.fetchSystem.output.entity.metadata.annotations['dock.tech/subdomain']
          }}/${{
            steps.fetchSystem.output.entity.metadata.annotations['dock.tech/system']
          }}/${{ parameters.environment }}/resources/aws/record-${{ parameters.recordName }}.${{ parameters.zoneName }}.yaml
        content: |
          metadata:
            annotations:
              dock.tech/scaffolder-parameters: ${{ parameters | dump }}
          spec:
            {%- if parameters.type != 'ALIAS' %}
            ttl: ${{ parameters.ttl }}
            values:
            {%- for v in parameters.values %}
              - ${{ v }}
            {%- endfor %}
            {%- else %}
            aliasTarget:
              dnsName: ${{ parameters.dnsName }}
              evaluateTargetHealth: ${{ parameters.evaluateTargetHealth }}
            {%- endif %}
            {%- if parameters.enableWeightedRouting %}
            setIdentifier: ${{ parameters.setIdentifier }}
            weight: ${{ parameters.weight }}
            {%- endif %}

    - id: openPR
      name: Open PR with edited record
      action: publish:github:pull-request
      input:
        repoUrl: github.com?owner=arthur-bryan&repo=aws-dns-crossplane-poc
        branchName: scaffolder/record-edit-${{ parameters.recordName }}-${{ parameters.zoneName }}-${{ parameters.environment }}
        targetBranchName: feature/ape-platform-alignment
        title: "chore(dns): update record ${{ parameters.recordName }}.${{ parameters.zoneName }}"
        description: |
          Scaffolder-generated edit to Record XR.

          System: ${{ parameters.system }}
          Environment: ${{ parameters.environment }}
          Record: ${{ parameters.recordName }}.${{ parameters.zoneName }} (${{ parameters.type }})

  output:
    links:
      - title: Pull Request
        url: ${{ steps.openPR.output.remoteUrl }}
